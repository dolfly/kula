// Model: Gemini 3.1 Pro (High)

1. add .gitignore
2. prepare docker integration inside ./docker/ with Dockerfile, build.sh script, docker-compose.yml example to monitor host system
3. add unit tests and make a check.sh that run linter and unit tests
4. add cross-compile scripts for arm64 and risc-v
5. The binary is missing proper usage message when --help flag is provided
6. Add man page for the binary
7. Add bash-completion script for the binary
8. Create a script that will make .deb package for Debian/Ubuntu systems (it should create /etc/kula/config.example.yaml, use /var/lib/kula for data and also install bash completion script)
9. Create also a script that will make a PKGBUILD for AUR package for Arch Linux (it should create /etc/kula/config.example.yaml, use /var/lib/kula for data and also install bash completion script)
