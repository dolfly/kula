// Model: Claude 4.6 Opus (Thinking)

Damn, you good. Hell of a work.
Let's make it even better!
1. If we only have gathered metrics for let's say 15 minutes then if we switch to the 30 minuts window the half of the chart should be simply empty, meaning for the time that we don't yyet have data for we should have empty graphs.
2. Add a alert feature. In case the time isn't synced, entropy is lower than 256 or load average exceeds the number of cores there should be an alert icon in the header indicating that, clicking on it should display a list of alerts
3. There should be also custom time window picker available that will allow to select a range of date/time
4. I like the current webui and I want to keep it, but I also would like to experiment a little bit. Can you please add option to switch between the current looks and an alterantive look, which will make all the graphs being displayed in separate lines (each should take full width so that one is under the other) + display live cpu and network usage as a bar graph isntead of gaguge.
5. The live guage of load average should labeled so the user know which one of the three is that value
6. memory graph and swap also doesn't have max value (max = total ram/swap value) and are missing "free/available"
7. add 3d time window preset
8. add packets per second metric to the network monitoring
9. add % next to used/total values in the memory and swap graphs

one more thing, I've also noticed that the webui shown around 765 Mbps while I was downloading a file, while wget shows 87,2MB/s (700 Mbps), check if we're calculating this correctly
