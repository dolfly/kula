// Model: Claude 4.6 Opus (Thinking)

<screenshots of webui and tui>

1. When I open webui it starts writing graphs from the moment I opened, not from the 5 minutes ago. It should rather use the default time window, load the metrics from the start of this time window, not the start of opening the browser.
2. When I switch between time windows the graphs freeze up to several seconds until it finally loads the data.
3. TUI - if the terminal height is too small to show all data it shows only the last fields like disks, tasks and sys, i would rather want it to show first fields first. Also maybe if the terminal is smaller we should make the data displayed compact, like only cpu usage (summary of all cores), memory without bar graphs and completly hide network and disks. Also in normal view by default hide the per core usage and let the user to toggle it with "a" key. Also in general please try to refine the asthetics / looks&feels of the tui, it's a bit chaotic right now.
4. The collector tries to create /var/lib/kula-szpiegula and place the data there. It shouldn't do it. Instead by default place it into data dir in the same place the binary is.

PS. I've installed the browser so it's available now in case you need it
